#lang info

(define collection "tests")

(define deps '())
(define build-deps '("base"
                     "rackunit"

                     "app-name-here"))

(define update-implie '("app-name-here"))
